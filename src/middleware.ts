// Middleware disabled for now to unblock deployment.
// Security features (Rate Limiting, CORS) will be integrated in a future update.
export function middleware() {
  return;
}

export const config = {
  matcher: [],
};
